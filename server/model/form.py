class Form:
    def __init__(self, union_id, form_id, expire_date, is_useful):
        self.union_id = union_id
        self.form_id = form_id
        self.expire_date = expire_date
        self.is_useful = is_useful
